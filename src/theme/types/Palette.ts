interface Palette {
  background: string;
  primary: string;
  secondary: string;
  inactive: string;
  text: string;
  card: string;
  border: string;
  shadow: string;
  red: string;
}

interface GlobalPalette {
  white: string;
  black: string;
}

export type {Palette, GlobalPalette};
